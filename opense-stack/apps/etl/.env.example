VITE_SUPABASE_URL=SUPABASE_URL_here
VITE_SUPABASE_ANON_KEY=SUPABASE_ANON_KEY_here

# Shared auth app URL (override if needed)
VITE_ACCOUNTS_URL=https://accounts.rcsn01.com

# ETL public URL used as post-login redirect target
VITE_APP_PUBLIC_URL=https://open-etl.rcsn01.com

# Optional: for scripts/check-workflows.ts (pnpm run db:check-workflows)
# SUPABASE_SERVICE_ROLE_KEY=eyJ... (from Dashboard > Settings > API)

# Supabase Cloud: Ensure "etl" is in Dashboard > Settings > API > Exposed schemas
# Otherwise workflows table won't be accessible via REST API.