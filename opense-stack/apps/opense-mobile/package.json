{
  "name": "@repo/opense-mobile",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build-assets": "node scripts/build-assets.cjs",
    "sync:ios": "pnpm build-assets && capacitor sync ios",
    "open:ios": "capacitor open ios",
    "run:ios": "pnpm build-assets && capacitor run ios",
    "test": "vitest run"
  },
  "dependencies": {
    "@capacitor/app": "^8.0.0",
    "@capacitor/browser": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
